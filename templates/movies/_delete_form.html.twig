<form method="post" action="{{ path('movies_delete', {'title': medium.title}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <button class="btn">Delete</button>
</form>
