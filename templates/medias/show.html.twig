{% extends 'base.html.twig' %}

{% block title %}Media{% endblock %}

{% block body %}
    <h1>Media</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ media.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ media.title }}</td>
            </tr>
            <tr>
                <th>Synopsis</th>
                <td>{{ media.synopsis }}</td>
            </tr>
            <tr>
                <th>Released_year</th>
                <td>{{ media.releasedYear }}</td>
            </tr>
            <tr>
                <th>Poster</th>
                <td>{{ media.poster }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('medias.crud') }}">back to list</a>

    <a href="{{ path('media_edit', {'id': media.id}) }}">edit</a>

    {{ include('media/_delete_form.html.twig') }}
{% endblock %}
